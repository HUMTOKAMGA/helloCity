{% extends 'base.html.twig' %}

{% block title 'Home | ' ~ parent() %}

{% block body %}
    <img src="{{asset('./img/franceFlag.JPG')}}" alt="" class="h-32 mt-12 rounded shadow-md">
    <h1 class="mt-5 text-3xl sm:text-5xl font-semibold text-indigo-600">Hello from Paris</h1>
    <p class="text-lg text-gray-800">It's currently {{ 'now'|date('h:i A', 'Europe/Paris')}}.</p>

{% endblock %}